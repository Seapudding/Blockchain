{
  "name": "uav-trusted-flight-blackbox",
  "version": "0.1.0",
  "private": true,
  "description": "ChainAir Blackbox: Fabric-based trusted UAV flight log notarization and accident traceability platform.",
  "workspaces": [
    "chaincode/uav-blackbox",
    "apps/api"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "start:api": "npm --workspace apps/api start",
    "demo": "node scripts/demo-flight.js"
  }
}

